<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grade Calculator</title>
</head>
<body>
    <h1>Enter Your Scores for 8 Courses</h1>
    <form action="/calculate" method="POST">
        <div>
            <% for (let i = 1; i <= 8; i++) { %>
                <label for="score<%= i %>">Course <%= i %>:</label>
                <input type="number" id="score<%= i %>" name="scores" min="0" max="100" ><br>
            <% } %>
        </div>
        <button type="submit">Calculate</button>
    </form>
</body>
</html>
